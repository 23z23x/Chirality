/* Purpose of this bar is to display the profile pictures/server images of all
   conversations. It should be able to be placed on any side of the screen and
   still function properly.
 */

import 'package:flutter/material.dart';

class MessageBar extends StatefulWidget {
  const MessageBar({super.key});

  @override
  State<MessageBar> createState() => _MessageBarState();
}

class _MessageBarState extends State<MessageBar> {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
    // List of buttons -> buttons have pfp on them -> on clicked take you to DMs
  }

}